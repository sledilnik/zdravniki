# Stage tracks the mainline `latest` image (multi-arch, rebuilt on every push
# to master by .github/workflows/build.yml). pullPolicy Always so a rollout
# picks up a freshly pushed `latest`.
image:
  tag: latest
  pullPolicy: Always

host: stage-zdravniki.sledilnik.org

env:
  REACT_APP_MODE: staging
